# Analysis parameters

AF35_PLATFORM_SPECIFICATION.tsv records the unchanged assay-specific transformations and scoring rules. Fixed numerical parameters, seeds and thresholds remain in the archived scripts. Their literals were not changed. No new cutoff, predictor, score or universal parameter file was inferred.
