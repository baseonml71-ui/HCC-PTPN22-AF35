# RC2 packaging boundary

RC2 remains NOT_READY. There are 59 current source candidates and zero approved ACTIVE_CURRENT analysis entrypoints. Excluding unclosed scripts prevents misleading execution claims; it does not satisfy the result-traceability gate.

## Frozen originals

The 59 files under scripts/frozen_originals are exact, already-sanitized rc1 Git blobs. They are not byte-identical to private workstation originals where rc1 changed local path literals. The original workstation files remain unchanged outside this repository. release/RC1_FROZEN_SOURCE_LINEAGE.tsv records both hash identities and the original relative artifact names. Full rc1 content is retained by v1.0.0-rc1. This boundary is a limitation against a literal request to redistribute all private workstation originals; their local-path/private-content exclusion is preserved.

## Allowed changes

Eight candidate scripts no longer copy private authorization text as administrative provenance. One associated authorization-only hash-list item was removed. The exact changed statements are recorded in release/PACKAGING_ONLY_CHANGE_RECORD.json. Mathematical calculations, scientific frozen-file guards, seeds, cohort filters and outcome-blind locks were retained. Sources were relocated without running them. No analysis, score, statistical estimate, manuscript or figure was regenerated.

## Source map interpretation

Source-table and primary-output names in release/PUBLIC_ANALYSIS_SOURCE_MAP.tsv are original-workspace-relative evidence identifiers unless explicitly included in the payload manifest. Hashes identify locally inspected artifacts. They are not promises that those files are redistributed or downloadable through a completed contract. Whole-figure hashes identify containers, not independently exported panels. Literal filename occurrences in SOURCE_SCRIPT_EVIDENCE.tsv are candidate evidence, never proof that the matching script produced a statistic. DISPLAY/ASSEMBLY code is not a numerical producer.

Only the fixed AF35 membership definition in Figure 2A is marked trace-complete, as a noncomputed definition. This does not certify the complete graphical rendering or a scoring pipeline. Other 40 main panels and all supporting supplementary rows remain incomplete. Included aggregate tables alone do not establish upstream reproducibility.

## Static audit limits

The derived-input ledger preserves every INPUT expression detected in the rc1 static I/O inventory plus all 43 included aggregate tables. That inventory is not a complete transitive runtime dependency detector: imports, computed filenames and helper-side reads may add dependencies. Parameter extraction records observed literal settings only, not a complete configuration migration. No unresolved candidate was promoted to ACTIVE_CURRENT.

Family README files provide one review starting point, not a fictional analysis launcher. Historical GSE287319 branches remain in three candidates; exclusion from execution is a containment measure, not completed branch extraction. Private scientific protocols and freeze records remain potentially necessary until each input contract is resolved.
